services:
  campus-web:
    image: ${WEB_DMZ_IMAGE:?set WEB_DMZ_IMAGE in .env}
    container_name: campus-web
    restart: unless-stopped
    ports:
      - "${WEB_DMZ_BIND_PORT:-80}:80"
    env_file:
      - .env
    environment:
      WAS_UPSTREAM: ${WAS_UPSTREAM:?set WAS_UPSTREAM in .env}
